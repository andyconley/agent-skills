# Implementation

## Changed

- Added shared final gates.
- Added shared pattern classes.
- Added strict mode to both writing skills.
- Bumped `humanizer` to `4.4.0`.
- Bumped `doc-flow-review` to `1.3.0`.
- Added regression fixtures under `examples/regression/`.
- Added manual test prompts under `tests/manual/`.
- Updated README and metadata to describe strict mode.

## Notes

- Vale is intentionally not included in this slice.
- Strict mode manually applies final gates and known surface tells.
- `SKILL.md` files include the core gates directly so the skills still work if only the main file is loaded.

