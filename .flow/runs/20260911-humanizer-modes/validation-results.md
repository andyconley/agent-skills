# Validation results

Status: implementation delivered under Andy Conley's explicit best-effort waiver. Releases v1.6.0 and v1.6.1 shipped; the installed-reference defect is fixed, and live behavioral failures remain documented. Updated 2026-09-14.

## Delivered skill

The delivered 4.8.1 skill retains one resolver in `skills/humanizer/SKILL.md` and one required combined `references/policy.md`. The failed selected-capsule layout was reverted under the recorded architecture stop condition. A subsequent bounded refinement changed the entry workflow to classify source facts and unknowns before editing, then compare each output claim and retained sentence against the source and selected mode. The combined policy now carries an explicit ten-class personal diagnostic inventory and a precise clarification rule for omitted material operational steps. The lint routing, corrected fixture oracle, neutral UI prompt, and approved documentation work remain.

Current product SHA-256:

- `skills/humanizer/SKILL.md`: `61c6d7577d99f8debc557b1abbbb066369e5462df14c45164ec62204f9c7f293`
- `skills/humanizer/references/policy.md`: `53a8a9b787777f4ce0d011fdb1dfd4075c943947801760f97abd8aa3c3565cf2`

## Mechanical evidence

`validation/mechanical.md` records twelve current-tree checks, all exit zero: shell syntax, three skill manifests, 27 fixture definitions, the ten-class personal inventory guard, missing/disconnected policy mutation checks, 18 installer checks through temporary runtime symlinks, lint-wrapper tests with real Vale and a routing mutation, workbreakdown contract in default and C locales, repository Vale on 50 files with zero findings, wrapper/manager help, skill quick validation, and `git diff --check`. These prove packaging and lint routing, not generated-prose behavior.

## Behavioral evidence

The user explicitly authorized sending unpublished candidate skill files and constructed writing-test prompts to authenticated Claude and Codex services. Raw prompts, outputs, event traces, stderr, and source-load metadata are under `validation/raw/`.

- Earlier combined-policy runs (`candidate-release`, `candidate-gated`, `candidate-closed`, `candidate-regression`) exposed source-correct Claude `journal-default` and `regression-word-choice` failures. One earlier `protection-engineering` run loaded installed humanizer 4.7 and was excluded as candidate evidence.
- The selected-capsule experiment (`candidate-v2-*`) had nine current-hash, source-admissible focused calls. Claude passed the journal default and personal controls but failed `regression-word-choice` and `protection-engineering`. Codex passed four paired controls. `validation/candidate-v2-adjudication.md` and `quality-review-resume.md` record the independent verdict. This layout was reverted; the outputs are historical experiment evidence.
- The restored combined-policy baseline (`candidate-combined-baseline`) repeated three Claude cases at the restored hashes. `journal-default` found the mirrored close and `protection-engineering` removed the wish. `regression-word-choice` resolved the ambiguous expiration through a question and removed the vague rollback claim, but incorrectly changed source `task` to `job` and added process commentary. A single good output for one case did not override earlier same-hash failures.
- The current workflow refinement (`candidate-workflow-focused`) produced five source-admissible calls in each runtime at the current hashes. Codex passed the inspected paired controls. Claude's default engineering `protection-engineering` kept the unprotected personal wish, and `regression-word-choice` still said `Rollback is fast.` despite no duration in the source. It left `job` and `task` distinct and asked about the six-month subject. The personal controls were clean/preserved. The current candidate therefore fails the focused release gate.
- An explicit-engineering diagnostic (`candidate-workflow-explicit-engineering`) on the same current hashes removed the wish and omitted the speed claim, suggesting a possible default-mode activation issue. Its runbook clarification supplied example referents absent from the source, so it did not establish full source fidelity. One A/B set is not causal proof.
- A session-only, namespaced Claude plugin copy tested native skill loading without changing the global installed link. The plugin's entry and policy hashes matched the current worktree, the native `Skill` invocation named `humanizer-candidate:humanizer`, the init event resolved that plugin to `/tmp/humanizer-native-candidate`, the candidate policy was read, and the global humanizer link still resolved to canonical 4.7 before and after. The native `protection-engineering` probe removed the wish, but the native `regression-word-choice` probe still wrote `Rollback is fast.` and suggested unsupported expiration referents. This is diagnostic surrogate evidence, not a personal-install acceptance result. Official Claude documentation describes session-only `--plugin-dir` testing and namespaced plugin skills: https://code.claude.com/docs/en/plugins .

## Complete suite and final affected-case retake

The complete `candidate-best-effort-full` suite ran all 27 fixtures in each runtime (54 calls). Every call exited successfully, read the expected candidate source, and met the runner's source-admission rule at entry hash `48749ba8...` and policy hash `d2f83771...`. An exact protected-span scan found no omitted protected spans. These calls preceded the final policy edit and are evidence of observed behavior, not validation of the final policy hash.

The independent test-engineer review scored Claude **22 pass, 5 fail**. Failures were `strict-personal` (strict status unreported), `strict-file` (required personal lint not invoked), `audit-formatting` (missed excess bold), `paired-engineering` (mirrored close retained), and `regression-word-choice` (six-month threshold/rollback uncertainty lost; `task` changed to `job`). The independent quality review scored Codex **25 pass, 2 fail**: `paired-engineering` lost the source's mistaken expectation, and `regression-word-choice` reduced `replacement config` to ambiguous `replacement`. See `validation/full-claude-review.md` and `quality-review-resume.md`. The strict-personal status verdict depends on interpreting the fixture's strict-status check for an inline prompt without a file target; it remains recorded as the reviewer's fail.

A narrow general policy change then required operational instructions to keep their named targets and conditions and required engineering rewrites to retain a source expectation that explains a correction. The policy hash for that retake was `860b0cf0...`. The `candidate-target-retake` run source-admitted all eight calls: `paired-engineering`, `regression-word-choice`, `personal-runbook`, and `regression-mixed` in both runtimes. Codex now keeps `replacement config`, the prior trail expectation, and the relevant runbook/rollback facts in these cases. Claude's runbook still asserts `The rollback is fast.` without measured duration and offers possible expiration referents absent from source. Its paired rewrite keeps the expectation but retains the author-state opening `My first read was wrong.`; its runbook also combines two actions in one step. The other affected compatibility cases retain their protected facts. Independent quality review scored the retake Codex 4/4 and Claude 2/4 for the four affected checks. This is a focused transfer check, not a new 54-case semantic pass. The static packaging/lint checks were rerun after the policy change and pass; `validation/mechanical.md` records the final hash.

The user-facing README names the best-effort limitations. Source accuracy in consequential procedures remains a manual review need. No failed live assertion is represented as passed.

## Published release and installed-path result

PR [#15](https://github.com/andyconley/agent-skills/pull/15) merged as Conventional Commit `111f8c9`; PR CI and the release workflow passed, and [v1.6.0](https://github.com/andyconley/agent-skills/releases/tag/v1.6.0) was published. The canonical checkout fast-forwarded to `v1.6.0` and `install.sh --all --no-vale` refreshed both runtime links. The installed entry/policy hashes matched the release, and the canonical static checks passed.

Fresh installed-path sessions source-admitted 16/16 sampled calls. Codex met all eight sampled checks; independent review scored Claude 4/8: failures in file-target strict validation, runbook uncertainty, engineering construction, and audit response shape. The `regression-word-choice` Claude output also reported three required shared files missing from the installed path. This is a newly discovered packaging failure, not a waived prose judgment. `validation/installed-4.8.0.md` records the case-level evidence. The 4.8.1 patch places readable local links at the paths named by the skill and adds package/installer checks. PR [#16](https://github.com/andyconley/agent-skills/pull/16) merged as Conventional Commit `5221c96`; PR CI, main CI, and release workflow passed, and [v1.6.1](https://github.com/andyconley/agent-skills/releases/tag/v1.6.1) was published. The canonical checkout and both runtime links match 4.8.1; all four packaged references resolve through each link. Fresh installed-path sessions admitted 10/10 calls at the final hashes. In the Claude `regression-word-choice` trace, all three formerly missing local shared files were read with content returned, closing the packaging defect. Independent review scored Codex 5/5 and Claude 1/5 on the selected affected cases; `validation/installed-4.8.1.md` lists the persistent semantic failures. The final mechanical suite passes on 50 Vale files. These final-hash installed checks supersede the 4.8.0 installed-path claim; they do not transfer a full semantic pass from the pre-patch 54-case suite.

## Delivery state

The best-effort release and packaging patch are delivered. Source-level and installed-path evidence, independent review, CI, release workflow, canonical sync, and both runtime installations are recorded above and in `HANDOFF.md`. The complete 27-case/two-runtime suite remains pre-patch behavioral evidence; no full final-hash semantic pass is claimed. The explicit best-effort deviation retains the observed Claude failures as risks. The Flow `mark-handback-ready` transition completed with this evidence. Archival, if requested, can use the tracked handback and readback artifacts.
