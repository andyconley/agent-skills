# Humanizer implementation shaping — proposed
Status: shaping; not an approved plan. Engineer confirmed one complete delivery with commit, PR/merge, installation and live Codex/Claude checks. Approved requirements.md and solution.md remain binding.

## Execution sequence
1. Baseline and isolate: record git status/HEAD, versions and installed symlink targets. Preserve current untracked run artifacts; carry them into a dedicated feature worktree without deleting originals or unrelated work. Run existing structural/installer/lint and representative engineering/doc-flow behavior checks; record pre-existing failures distinctly. Implement in the worktree so canonical-source symlinks do not load unfinished edits.
2. Create six constructed fixture groups with input, protected spans, named voice traits, seeded defects and expected policy/disposition. Keep intentional prose data separate from governing instructions. No full-output exact snapshots or model score.
3. Refactor humanizer into a concise entry point and required local policy file at skills/humanizer/references/policy.md. Entry point explicitly loads the policy. SKILL.md alone owns resolution order and workflow; the support file owns mode definitions, the purpose/protection matrix and engineering/personal pattern dispositions. Do not duplicate the resolver. Preserve hard protection rules in entry point as fallback; missing policy yields actionable inability to complete, not silent engineering rewriting or invented rules. Consolidate workflow, fix contradictory examples, add audit and targeted diagnostics. Avoid personal exceptions in shared generic rules; if a reference needs scoping, limit it to humanizer artifact output and verify shared behavior unchanged.
4. Align lint wrapper/CI and sample classification. Proposed interface: scripts/lint-prose.sh --profile repository|engineering|personal [--] [files...]. Default repository selects deterministic repository prose targets; engineering/personal require explicit nonempty files. Unknown profile/flags, nonexistent targets and tool/config failures produce nonzero diagnostic errors. Personal config explicitly selects only suitable advisory candidates from existing rule owners (initial candidate: CorporateFiller), with no inherited AuthorState, MirroredRhythm, PlainVerbs or TermDrift. A candidate is never an edit mandate; retain protected material and intentional voice. Real Vale probes must establish the config works rather than trusting wrapper mocks. Do not equate no applicable mechanical checks with semantic success. Existing CLI compatibility is not required, but all docs/callers update atomically. CI installs a pinned Vale version and invokes the same wrapper/profile mapping rather than separate Vale target selection. Keep governing docs in tests/manual and examples/regression linted. Move intentional sample data to tests/fixtures/humanizer/ excluded from repository allowlist, or fenced source blocks whose exclusion is proven with real Vale. Never exempt the complete manual/regression directories.
5. Update docs, supported prompts, manifest description, agent metadata and consistent skill version. Preserve doc-flow-review version unless its behavior actually changes (not intended). Choose humanizer semver from actual API/behavior delta; no invented repo release version. Required metadata/version validation must agree. Do not hand-edit generated changelog; semantic-release owns it.
6. Validate and independently review complete change. Reconcile all material findings. Commit Conventional Commit(s), open PR with factual behavior and evidence, wait for relevant CI, merge only approved complete passing change, verify remote merge/release, sync canonical checkout safely, install Codex/Claude and run fresh installed live tests.

## Behavioral contract
R1-R12 in requirements.md are authoritative. Resolve protection/explicit constraints, substantive operational purpose, requested mode, operation, intensity, semantic gates and applicable mechanical checks in that order. Default engineering; explicit personal and clear equivalent intent only; actual operational/reference purpose retains engineering; incidental exact material remains protected inside personal prose. Operation edit/audit independent of normal/strict. All sweeps still run, personal disposition requires reader impact instead of pattern match alone. Audit no replacement draft or authorship score. Clean audit yields no findings. Sample optional. Strict validates effective policy and truth without using engineering lint to erase personal voice. Commentary/doc-flow-review retains shared discipline.
Errors/empty states: no draft -> ask for draft; materially ambiguous operational purpose -> one focused question; clean rewrite -> preserve effective text; clean audit -> no actionable findings; missing local policy -> explicit failure rather than fabricated compliance; unavailable Vale -> manual semantic gates with accurate unavailable state, script returns tool error.

## File map
Primary: skills/humanizer/SKILL.md, new references/policy.md, VERSION, agents/openai.yaml, skills/manifest.tsv.
Validation: scripts/lint-prose.sh, new tests/lint-prose-test.sh, tools/vale/.vale.ini and profile configs, .github/workflows/ci.yml. Reuse rule ownership; no duplicate vocabulary regexes solely for a profile.
Examples/docs: examples/regression/, tests/manual/humanizer.md, tests/manual/README.md, README.md, tools/vale/README.md; update PR template/help only as changed commands require.
Packaging proof: scripts/validate-skills.sh and tests/install-test.sh should check required local support references/version consistency and resolved temporary installed paths. Avoid installer redesign.
Shared files: preserve contract; small scoped clarifications only if required to remove competing humanizer instructions. Existing doc-flow-review source and tests remain guardrails.

## Ownership and execution
One implementation owner for policy, docs and fixture topology; a separate bounded lint owner is optional only with disjoint write paths and frozen interface. Serialize shared-file/CI changes. Independent quality reviewer checks technical preservation, minimum edits, source/fixture classification and delivery evidence. Planning core reviews: BA, PM and architect. Implementation manifest must record exact write scopes and capabilities before dispatch.

## Delivery details
Canonical origin https://github.com/andyconley/agent-skills.git; current branch main. install.sh pulls and refuses dirty checkout; run artifacts must be committed or safely preserved before invoking. Use temporary manager install targets for premerge path checks; don't repoint global skill links to a worktree. After merge, sync verified main, run ./install.sh --all --no-vale (Vale already present; recheck), verify links/version/module readback. Existing symlink mismatch is an explicit repair decision, not a force-overwrite.
Fresh runtime checks use actual installed skills and modules. Codex exec and Claude print exist; commands/auth availability is not proof of successful loaded-skill behavior. Save prompts/output and runtime/version/path evidence. Do not paste the entire skill as a substitute for loading. No unsafe permission bypass needed for read-only rewrite/audit samples.

## Remaining plan decisions
Core role findings and engineer acceptance of this shaped plan. No implementation approval transition yet. Prior scope approval authorizes planned eventual PR/merge/install, so do not ask that again.

## Review dispositions
BA: classify purpose for audit; include explicit default audit examples, omission location with repair direction, operational-task metadata and reviewer-facing reasons for changed spans. Accepted; validation-plan.md carries checks.
PM: one complete release with internal checkpoints and fresh installed proof. Accepted.
Architect: one resolver in SKILL.md; local matrix only; narrow personal lint with errors distinct from findings; preserve governing fixture prose lint; coherent atomic delivery. Accepted.
No unresolved scope or architecture question. Engineer review of this shaped plan is pending.

## Model advice
Coordinator judgment gpt-5.6-sol/high provisional, parent unknown, no switch. Core role routing declarations: BA and PM gpt-5.6-terra/medium; architect gpt-5.6-sol/medium. Runtime model identities not separately observed. Scope fits these roles; no additional background research or new evaluation service.
