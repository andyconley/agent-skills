## Review Summary

**Verdict:** REQUEST CHANGES

**Overview:** The selected-mode split is a bounded, coherent response to earlier observed mode drift. The current static checks pass, but the split has not been exercised in a live Codex or Claude candidate run; the previous live outputs belong to an earlier layout. Release and installation are not ready.

### Critical Issues

- None.

### Important Issues

- [skills/humanizer/references/personal.md:3] Personal mode is required to diagnose every construction class under R5, but the split deliberately avoids the shared engineering definitions, and the personal capsule never names or defines the complete classes. The matrix only says “Diagnose every class.” A model loading the required personal files has no operational class-by-class sweep to follow. Add a concise personal diagnostic inventory or locally scoped definitions for the same classes in this capsule, keeping the personal reader-impact disposition and without loading engineering mandates. Extend the structural test to assert that the personal inventory stays connected.
- [.flow/runs/20260911-humanizer-modes/implementation-architecture.md:97] The selected-capsule experiment and subsequent full two-runtime candidate suite have not run on this layout. The previous valid Claude failures in default journal audit and ambiguous runbook word choice therefore remain unresolved, while the previous protection-engineering output was an invalid test because it loaded installed 4.7. Use the planned source-read gate, then inspect the targeted paired cases and complete candidate/regression suite on exact final hashes. Do not infer a behavioral pass from structural tests, prior-layout Codex passes, or process exit codes.
- [.flow/runs/20260911-humanizer-modes/validation-results.md:5] The durable validation account still reports 22 fixtures and 47 Vale files, and says only `policy.md` is required. The current tree has 27 fixtures, 49 Vale files, and three required references. Regenerate the mechanical and validation summaries from the final tree, mark all old candidate outputs as pre-split evidence, and record the automatic approval-review block on the unpublished-candidate Claude run. This is required for a reviewer to distinguish present static proof from unavailable live proof.

### Suggestions

- [skills/humanizer/agents/openai.yaml:4] The UI default prompt correctly leaves mode unspecified, so it selects engineering by default. The generic wording does not advertise the new personal or audit paths; consider adding a short UI hint outside the prompt if discovery matters, while keeping this prompt unambiguous.
- [tests/fixtures/humanizer/cases.json:491] The corrected regression oracle appropriately keeps `job` and `task` distinct unless evidence links them, while treating `folder` and `directory` as the same path. Maintain this distinction when judging live outputs; an apparent reduction in term count is not proof of semantic preservation.

### What's Done Well

- [skills/humanizer/SKILL.md:20] One entry point owns protection, purpose, mode, operation, and intensity resolution. It requires exactly one mode capsule and prevents a personal narrative's incidental command from silently changing policy.
- [skills/humanizer/references/policy.md:5] Common protections, the policy matrix, portability/deletion diagnostics, audit shape, strict behavior, and response gates remain local to humanizer. The engineering capsule retains the full construction sweep and technical exceptions; the personal capsule retains minimum-effective-edit and voice-preservation rules.
- [scripts/validate-skills.sh:47] Packaging rejects missing or disconnected common and selected-mode references. Installer checks verify that all three resolve through both temporary runtime links.
- [scripts/lint-prose.sh:1] Repository, engineering, and advisory personal profiles share one wrapper, and CI calls it. The narrow bare-target compatibility path preserves doc-flow-review's strict caller. Shared and doc-flow-review source files remain unchanged.
- [tests/manual/humanizer.md:94] The job/task oracle correction removes an unsound requirement to merge potentially distinct concepts; `folder`/`directory` remain a supported same-path normalization.

### Verification Story

- Tests reviewed: yes. I independently reran fixture-schema and packaging mutations (27 cases), skill validation (3 skills), lint-wrapper tests, installer tests (18 checks), repository Vale (49 files, zero findings), and `git diff --check`; all passed. I inspected the split references, entry point, relevant documentation/metadata, and the product diff. These tests do not judge generated prose.
- Build/runtime checks reviewed: prior-layout candidate results only. No live candidate output exists for the current split. Automatic approval review rejected sending the unpublished candidate skill to Claude, so I did not attempt an alternate external CLI path. No remote CI, release, canonical sync, installation, or post-install live check is verified.
- Structural fit reviewed: yes. The additional references stay inside the approved humanizer-local boundary and match the reversible layout experiment. The accepted one-file solution is superseded only as an implementation layout choice described in the architecture note; the resolver remains singular. No implementation commit exists yet, so Conventional Commit review is not applicable.
- Safety reviewed: yes. This is prose policy, local lint routing, and packaging validation; auth, secrets, persistent data, migrations, and production operations are not changed. Live-run disclosure remains blocked by automatic approval review.
- Remaining risks: The new layout may improve instruction isolation, but this is an untested causal hypothesis. Missing personal diagnostic classes and stale validation records must be fixed locally; semantic release claims require permitted, source-verified live evidence from both runtimes.

### Addendum — 2026-09-14

The first Important Issue above is resolved at the instruction and structural-test level. [personal.md] now names and describes all ten construction classes, directs a separate pass across the artifact, and treats each match as a diagnostic cue subject to reader impact. It does not import the engineering-only definitions or their default rewrite disposition. [tests/humanizer-fixture-test.py] checks that the complete inventory and diagnostic wording remain present; I reran it successfully (27 fixtures, missing/disconnected-reference mutations, ten-class guard). This does not prove that a live model will apply the sweep correctly.

**Verdict remains REQUEST CHANGES.** The split still lacks permitted, source-verified live candidate results, and the durable validation record still describes the earlier tree. The previous “missing personal diagnostic classes” residual risk is closed; the live-behavior and evidence gaps remain.

### Addendum — 2026-09-14, validation record

The stale-record finding is resolved. I compared the refreshed [validation-results.md](validation-results.md) and [validation/mechanical.md](validation/mechanical.md) with the current source: all four recorded SHA-256 hashes match; the record names 27 fixtures, the ten-class guard, three required references, 18 installer checks, and repository Vale's 49 files. It explicitly labels the older outputs as pre-split and the new layout as untested. The record also states the automatic approval-review reason and does not claim a successful post-split external run.

**Verdict remains REQUEST CHANGES** solely on acceptance readiness: the split needs permitted, source-verified live candidate behavior in Codex and Claude before release. The first and third Important Issues in this review are closed by the two addenda; the live-evidence issue remains.

### Addendum — 2026-09-14, targeted live review

The new gate admits all nine inspected runs: each read the expected worktree entry, common policy, and selected capsule at the recorded hashes; none read the other capsule or invoked an installed native skill. I compared the captured final outputs with the fixture checks and current source hashes.

- Claude `journal-default` identifies the mirrored closing, while `audit-personal` reports no finding; Codex makes the same mode distinction. Claude also proposes that the trail “appeared to end” when the source says only that the narrator *thought* it ended. That repair direction risks inventing an observation, and its two author-state findings go beyond the fixture's required mirrored-closing diagnosis. Keep this as an audit-precision concern, not the primary stop reason.
- Claude `protection-personal` preserves the complete source and protected spans, as does Codex. Codex `protection-engineering` removes the unprotected wish and preserves the authored action. Claude `protection-engineering` returns the personal text unchanged, including `I wanted **one** quiet evening.` The engineering and personal outputs are identical despite loading different capsules. This fails the mode-specific disposition.
- Claude `regression-word-choice` uses plain verbs and keeps `job` and `task` distinct while naming the shared path `directory`. It still changes `pretty fast` into `Rollback is fast`, a stronger unsupported assertion where the oracle requires an unknown duration. Its six-month sentence also leaves `it` without a resolved referent in an operational instruction. This fails truth/uncertainty and executable clarity.

**Verdict: REQUEST CHANGES.** The first targeted Claude battery fails two material assertions. Under [implementation-architecture.md](implementation-architecture.md)'s explicit stop condition, revert the layout experiment as one unit and return to architecture review; do not count these runs as a passing experiment or proceed to the focused repeats or full release suite. Codex paired controls pass, which narrows the observed failure to Claude behavior without proving the architecture hypothesis. Earlier addenda accurately close the static inventory and record issues; the live-behavior issue is now a demonstrated failure rather than merely unverified.

### Addendum — 2026-09-14, combined workflow refinement

The split experiment has been reverted. I reviewed the current combined policy at entry SHA-256 `48749ba8ef8aa949de5c32724536d8c9d2eb9b8ff297ca2d2b7ba513602c64f9` and policy SHA-256 `d2f83771b2f8b5a8be6fa8673e88e568fbc14dd2b51709d5ed34b2a5574d2c71`. The source-read metadata and captured traces for the workflow-focused and explicit-engineering runs show the requested entry and policy loaded without a stale native skill. The older combined-baseline runs used different hashes and cannot validate the current refinement.

- **Default engineering fails in Claude.** The workflow-focused `protection-engineering` output retains the unprotected wish `I wanted **one** quiet evening.` unchanged; its personal control also retains it. `regression-word-choice` still changes `pretty fast` to the unsupported assertion `Rollback is fast`, although it correctly keeps `job`/`task` distinct and asks about the six-month referent. These are direct failures of the approved default-mode and truth/uncertainty checks. The corresponding Codex focused outputs satisfy these checks.
- **The journal contrast is mostly demonstrated.** Both runtimes report the mirrored closing in default engineering audit and no issue in explicit personal audit. Claude's suggested repair says the trail “appeared to end,” a physical observation absent from a source that reports only the narrator's mistaken belief. This is a secondary audit-grounding problem, not evidence that default mode was unselected.
- **Explicit-mode prompts do not repair the default path.** Claude's explicit-engineering protection case removes the wish but also removes the authored `I wrote` attribution. Its explicit-engineering regression drops the rollback claim without stating the duration is unknown, and offers possible identities for the unresolved six-month item. The approved UI/default use and fixture require correct behavior without adding an explicit engineering instruction.
- **Native plugin evidence is a surrogate.** The namespaced `humanizer-candidate:humanizer` invocation loaded a temporary candidate copy whose entry/policy hashes match the current worktree; the global humanizer link stayed unchanged. This proves native loading can exercise these candidate bytes, but it is not the planned post-install canonical-path check or complete installed-runtime suite. Its output again says `Rollback is fast`, corroborating the default-path failure rather than clearing it.

**Verdict: REQUEST CHANGES.** No current combined-policy candidate is release-ready. The focused suite fails substantive assertions, the earlier combined baseline is mixed, and neither explicit-mode prompts nor a temporary native-plugin surrogate substitute for passing default behavior and the complete two-runtime release gate. Revisit the engineering decision boundary and test design before another broad suite or publication; do not claim that the workflow refinement solved the observed Claude failures.

### Addendum — 2026-09-14, preliminary best-effort release review

Andy Conley explicitly selected a best-effort release with known behavioral failures documented. That supersedes the prior all-assertions-pass gate; the old strict verdicts above are historical, not a reason by themselves to stop release. I reviewed the complete 27-case/two-runtime candidate suite at entry hash `48749ba8...` and policy hash `d2f83771...`: all 54 calls have successful source-read admission. Local shell syntax, skill validation (3), fixture/negative tests (27), installer (18), lint-wrapper tests, repository Vale (47 files, zero findings), and `git diff --check` pass. The raw event logs are ignored by Git, and a local scan of non-raw run artifacts found no common credential or private-key markers. These checks do not prove editorial reliability or remote CI.

**Codex case disposition:** 25 of 27 meet their fixture checks on the inspected final output. Pass: `engineering`, `natural-default`, `personal`, `equivalent`, `personal-clean`, `personal-technical`, `personal-runbook`, `audit-engineering`, `audit-personal`, `audit-clean`, `strict-personal`, `portability`, `ambiguous`, `doc-flow`, `journal-default`, `audit-omission`, `strict-file`, `strict-restricted`, `protection-engineering`, `protection-personal`, `audit-formatting`, `regression-protected`, `regression-mixed`, `doc-flow-strict`, and `doc-flow-normal`. Fail: `paired-engineering` removes the source's mistaken trail expectation while preserving only the corrected route and continued walk; `regression-word-choice` changes the operational target from “replacement config” to ambiguous “replacement.” Its other checks, including source `job`/`task` distinction, six-month uncertainty, and unspecified rollback duration, pass. The `strict-file` trace shows actual invocation of the personal lint profile; `strict-restricted` accurately reports that mechanical validation was not run.

The Codex runbook target loss is a newly observed technical-meaning risk, separate from the previously waived Claude outcomes. Under [acceptance-deviation.md](acceptance-deviation.md), it needs a fix or an explicit risk disposition before merge. The engineering trail case is a meaning-loss limitation and must be added to best-effort evidence if it is not fixed. Claude's full-suite `paired-engineering` and `audit-formatting` outputs also miss their checks, and `regression-word-choice` again changes `task` to `job` while omitting the rollback-duration unknown; the test engineer is adjudicating Claude independently. Claude's `protection-engineering` passes in this full run, consistent with observed variability rather than a universal fix.

**Provisional verdict: REQUEST CHANGES.** The known Claude failures are waived as documented behavior, but the new Codex operational-target loss requires disposition, the user-facing README/release description still need to describe observed best-effort limitations, and remote CI, merge, canonical installation, and installed-client checks have not run. A planned general rule to keep an operational action's source target noun phrase can address the new defect; after any source edit, label this 54-case suite as pre-change evidence and rerun affected cases at the new hashes.

### Addendum — 2026-09-14, final-policy affected-case retake

I reviewed the eight final outputs and source-admission metadata under `validation/raw/candidate-target-retake/`. All eight calls exited zero and read the intended candidate source. The current entry SHA-256 is `48749ba8ef8aa949de5c32724536d8c9d2eb9b8ff297ca2d2b7ba513602c64f9`; the final combined-policy SHA-256 is `860b0cf0ee4071628aa2740e9e5c2d187ee4f76b40d2fb8807650ef2e75838d3`. The new policy instruction to retain each operational action's named target and source condition is general and fits the engineering procedure branch. The full 54-case suite in the preceding addendum used the earlier policy and remains pre-change evidence.

**Codex: 4/4 affected cases pass their fixture checks.** `paired-engineering` now retains the narrator's mistaken expectation that the trail ended at the creek and the continued walk, while removing the mirrored close. `regression-word-choice` explicitly says `replacement config`, keeps `job` and `task` distinct, ties both references to the same directory, preserves the unresolved six-month question, and says the rollback duration is unspecified. `personal-runbook` keeps the negative `retry.log` obligation and 15-second timeout; `regression-mixed` keeps the cron argument, three lost runs, and executable rollback actions. The two Codex defects identified in the complete pre-change suite are fixed in this bounded retake, without an observed regression in its two procedure controls. This does not establish a 27-case pass at the final hash.

**Claude: 2/4 affected cases pass.** `personal-runbook` and `regression-mixed` retain their protected facts and plain procedure verbs. `paired-engineering` now retains the mistaken expectation, but leaves the seeded author-state opener `My first read was wrong.`; this fails its engineering-disposition check. `regression-word-choice` keeps the `config` target and the `job`/`task` distinction, but still upgrades `pretty fast` to the unsupported claim `The rollback is fast.` It also combines disable/configure in one numbered step and proposes possible six-month referents absent from the source instead of resolving the ambiguity. The unsupported rollback certainty is the material factual failure; the step shape and suggested referents are narrower clarity/grounding concerns. No new loss of the protected runbook facts appears in these retakes.

**Bounded verdict: APPROVE the general target-retention correction for a best-effort PR under the user's explicit behavioral waiver.** There is no new unwaived product or packaging blocker in these eight outputs. The Claude failures remain real and must stay visible in the validation record and release description; the focused Codex success must not be described as a full final-hash semantic pass. The README and `validation-results.md` now distinguish earlier Codex failures from the focused retake and identify the best-effort limits. Remote CI, merge, canonical installation, and fresh installed-client checks remain unverified delivery steps, so this addendum does not approve a completed release.

### Addendum — 2026-09-14, 4.8.1 installed-reference repair

**Verdict: APPROVE the packaging patch, pending an installed Claude check.** The 4.8.0 failure was that Claude could not find required shared references from the installed humanizer skill. The 4.8.1 entry now asks for `references/pattern-classes.md`, `references/final-gates.md`, and `references/agent-output-discipline.md` under the skill's own apparent path; the policy's class link is local to its `references` directory. The three new local symlinks resolve to the existing repository `shared/` sources. The version in front matter, body, and `VERSION` agrees at 4.8.1. No substantive writing policy changed.

I independently created temporary Codex and Claude skill links to this checkout and resolved all four required references through each installed-link path; each exists and resolves to the intended policy or shared source. I also reran `scripts/validate-skills.sh` (3 skills), `tests/install-test.sh` (18 checks), `tests/humanizer-fixture-test.py` (27 fixtures plus missing/disconnected policy faults), and `git diff --check`; all passed. The validator now rejects a missing or content-divergent packaged shared reference, and the installer test compares all four readable references through both runtime links. These checks cover the repository's supported symlink-to-checkout installation path.

**Residual risk:** File-system resolution and `cmp` do not prove Claude's skill loader will read the local reference symlinks in a fresh installed session. The earlier failure was observed in Claude, so verify that client explicitly after installing 4.8.1 and record the loaded paths before claiming the incident resolved in production. The tests also do not establish that a standalone copy of only `skills/humanizer/` would carry the symlink targets; that is outside the current installer contract. No auth, data, or operational safety dimension changes in this patch; Conventional Commit review is pending a commit.

#### Installed 4.8.0 smoke evidence reviewed

I independently scored the 16 `installed-best-effort-smoke` outputs against their fixture checks and inspected the strict-file traces. These runs loaded installed 4.8.0 entry/policy hashes `48749ba8...`/`860b0cf0...`, **before** the 4.8.1 packaging patch. They are evidence for the incident and behavioral limits, not a live verification of the new symlinks.

- **Codex: 8/8 pass.** The audit flags blanket bold without objecting to the purposeful `**one**`; the natural/default, paired, runbook, personal, protection, and word-choice cases preserve their specified facts and mode decisions. Its strict-file trace invokes `lint-prose.sh --profile personal` on the target file, then returns the duplicate-free draft.
- **Claude: 4/8 pass.** `natural-default`, `personal-runbook`, `personal`, and `protection-engineering` meet their fixture checks. `paired-engineering` retains the author-state framing (`My first read of the trail was wrong`). `audit-formatting` adds a second finding against the purposeful `**one**`, treating it as suspect by association instead of preserving that intentional emphasis. `regression-word-choice` omits the required statement that rollback duration is unknown; its preamble also says the absent shared definitions do not affect the result and claims a complete sweep despite not loading them. `strict-file` removes the duplicate but never invokes the required personal lint profile; its trace only reads the draft and policy, attempts the unavailable shared reference, and lists the absent shared directory.

The Claude `regression-word-choice` output explicitly reports that its installed skill has no shared class/gate files, and the `strict-file` trace independently shows a failed attempt under `/Users/andyconley/.claude/skills/shared/`. This is direct evidence that the 4.8.0 path was broken in the installed Claude layout. The four Claude fixture failures remain known best-effort behavior, but the missing-reference incident itself needs a fresh 4.8.1 installed Claude read of the local links before closure. No assertion in this review treats the 4.8.0 smoke as a 4.8.1 semantic pass.

### Addendum — 2026-09-14, installed 4.8.1 follow-up

**Packaging incident: resolved in the installed Claude read.** I inspected the `installed-481-followup` traces at current entry/policy SHA-256 `61c6d757...`/`53a8a9b7...`. All ten runs exited zero and met source admission. In Claude's `regression-word-choice` trace, separate `Read` calls returned actual contents from `/Users/andyconley/.claude/skills/humanizer/references/pattern-classes.md`, `final-gates.md`, and `agent-output-discipline.md`; the policy and entry also loaded. Codex's installed-link trace read the same three local paths successfully. This closes the specific 4.8.0 missing-reference path failure for the supported installed layout. It does not imply that the model obeyed every loaded instruction.

**Fixture and response-contract disposition:** Codex passes all five cases (`audit-formatting`, `paired-engineering`, `personal-runbook`, `regression-word-choice`, `strict-file`). Its `strict-file` trace actually runs the personal lint profile on the draft and returns the duplicate-free text. Claude passes `audit-formatting`: it flags diffuse bold in the first sentence and treats the single bold `**one**` in the second as defensible. Claude fails the other four overall. `paired-engineering` retains the source's mirrored close (`It kept going, and so did I`) despite preserving the mistaken trail expectation. `personal-runbook` produces a sound procedure with `retry.log` and the 15-second timeout but then adds a method/policy explanation after the artifact; this violates the edit response gate. `regression-word-choice` again changes `pretty fast` into the unsupported assertion `The rollback is fast.`; it also gives ungrounded possible six-month referents and puts two operational actions in one numbered step. `strict-file` removes the duplicate but has no personal lint invocation in its trace, so the strict-validation check fails. Claude's 4.8.1 follow-up is therefore **1/5 fully compliant**, even though its personal runbook artifact alone meets the content checks.

**Verdict: APPROVE the 4.8.1 reference-path repair; retain the best-effort behavioral waiver and documented failures.** The installed traces verify the packaging fix in both runtimes. The remaining Claude deviations are execution and response-quality failures, not evidence of missing reference files. They should remain explicit in release validation; do not describe the five-case follow-up as a semantic pass. No production data, authentication, or migration dimension applies to this local writing-skill patch.
