# Implementation Sequencing

The lead-developer confirmed four delivery slices: registry compatibility, Story v3 and fixtures, lifecycle and distribution guidance, then validation and handback. The existing Story v2 hash remains pinned. The contract harness was renamed to cover all template generations.

Commit boundary: one `feat(workbreakdown)` commit because the template, contract, tests, and documentation form one user-visible capability.
